syntax = "proto3";

// buf:lint:ignore PACKAGE_DIRECTORY_MATCH
// buf:lint:ignore PACKAGE_VERSION_SUFFIX
package alchemy.multipart;

option go_package = "multipart";

message Multipart {
  repeated uint64 ptr = 1;
}
